#include <iostream>
#include <vector>
#include <map>
#include <cstring>

class Room {
 public:
  Room(char description[50]);
  char description;
  map<char[], Room> exits;
  vector<Item*>* items = new vector<Item*>;
  void setExt(char direction[10], Room neighbor);
  void printShortDescription();
  void printLongDescription();
  void printExits();
  void printRoomItems();
}
